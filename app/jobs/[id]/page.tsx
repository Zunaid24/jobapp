"use client";
import { useEffect, useState } from "react";
import { useParams, useRouter } from "next/navigation";
type Person={id:string;name:string;title?:string|null;email?:string|null;linkedin_url?:string|null};
type Job={id:string;title:string;company:string;location:string;type:string;description?:string|null;apply_url?:string|null;posted_at?:string|null};
type Draft={subject:string;body:string;fitScore?:number;fitReasons?:string[];cvName?:string|null};
export default function JobContacts(){const params=useParams<{id:string}>();const router=useRouter();const [job,setJob]=useState<Job|null>(null);const [company,setCompany]=useState<any>(null);const [people,setPeople]=useState<Person[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState("");const [drafts,setDrafts]=useState<Record<string,Draft>>({});const [common,setCommon]=useState<Draft|null>(null);const [modal,setModal]=useState<"jd"|"personal"|"common"|null>(null);const [active,setActive]=useState<Person|null>(null);const [busy,setBusy]=useState(false);const [notice,setNotice]=useState("");
 useEffect(()=>{(async()=>{try{const r=await fetch(`/api/jobs/${params.id}/contacts`,{method:"POST",cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to enrich contacts");setJob(d.job);setCompany(d.company);setPeople((d.decisionMakers||[]).slice(0,10));}catch(e){setError(e instanceof Error?e.message:"Unable to load contacts");}finally{setLoading(false);}})()},[params.id]);
 const generatePersonal=async(p:Person)=>{setActive(p);setModal("personal");if(drafts[p.id])return;setBusy(true);try{const r=await fetch("/api/gemini/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({job:{...job,contact_email:p.email,decision_maker_name:p.name,decision_maker_title:p.title}})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to generate email");setDrafts(x=>({...x,[p.id]:d}));}catch(e){setNotice(e instanceof Error?e.message:"Unable to generate email");setModal(null);}finally{setBusy(false);}};
 const generateCommon=async()=>{setModal("common");if(common)return;setBusy(true);try{const r=await fetch("/api/gemini/common",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({job,contacts:people})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to generate common email");setCommon(d);}catch(e){setNotice(e instanceof Error?e.message:"Unable to generate common email");setModal(null);}finally{setBusy(false);}};
 const send=async(to:string[],draft:Draft)=>{if(!draft||!to.length)return;setBusy(true);try{const r=await fetch("/api/gmail/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({bcc:to,subject:draft.subject,text:draft.body})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to send");if(job){await fetch("/api/tracker",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({jobId:job.id,jobTitle:job.title,company:job.company,location:job.location,status:"Applied",subject:draft.subject,applicationBody:draft.body})});}setNotice(`Email sent to ${to.length} recipient${to.length===1?"":"s"}.`);setModal(null);}catch(e){setNotice(e instanceof Error?e.message:"Unable to send email");}finally{setBusy(false);}};
 if(loading)return <main className="app-shell"><div className="empty-state">Finding the best HR and Talent Acquisition contacts for this company…</div></main>;
 if(error||!job)return <main className="app-shell"><button className="secondary-button" onClick={()=>router.back()}>← Back</button><div className="notice">{error||"Job not found"}</div></main>;
 return <main className="app-shell"><header className="topbar"><div><button className="text-link" onClick={()=>router.back()}>← Jobs</button><span className="eyebrow">SELECTED JOB</span><h1>{job.title}</h1><p>{job.company} · {job.location}</p></div></header>{notice&&<div className="notice">{notice}</div>}<section className="job-detail-card"><div className="job-card-top"><div><h2>{job.title}</h2><p>{job.company}</p></div><span className="match">{job.posted_at?new Date(job.posted_at).toLocaleDateString():"Recent"}</span></div><div className="job-meta"><span>{job.location}</span><span>·</span><span>{job.type}</span></div><div className="detail-actions"><button className="secondary-button" onClick={()=>setModal("jd")}>View JD</button>{job.apply_url&&<a className="primary-button" href={job.apply_url} target="_blank" rel="noreferrer">Apply on company page</a>}</div></section><section className="contacts-section"><div className="section-heading"><div><span className="eyebrow">TARGET CONTACTS</span><h2>HR & Talent Acquisition</h2></div><span className="job-count">{people.length}/10</span></div><p className="profile-help">These contacts are enriched only because you opened this job. Prioritized toward recruiters, Talent Acquisition and HR hiring contacts.</p>{people.length?<div className="decision-maker-list">{people.map(p=><article className="decision-person" key={p.id}><div><strong>{p.name}</strong><span>{p.title||"Hiring contact"}</span><span className="contact-email">{p.email||"No email found"}</span></div>{p.linkedin_url&&<a className="text-link" href={p.linkedin_url} target="_blank" rel="noreferrer">LinkedIn</a>}<button className="primary-button compact" disabled={!p.email||busy} onClick={()=>generatePersonal(p)}>{drafts[p.id]?"View Email":"Generate Email"}</button></article>)}</div>:<div className="empty-state">No suitable HR/TA contacts with email were found for this company.</div>}<button className="primary-button common-email-button" disabled={!people.some(p=>p.email)||busy} onClick={generateCommon}>Generate Common Email</button></section>{modal&&<div className="modal-backdrop" onClick={()=>!busy&&setModal(null)}><div className="modal" onClick={e=>e.stopPropagation()}>{modal==="jd"&&<><span className="eyebrow">JOB DESCRIPTION</span><h2>{job.title}</h2><p>{job.company}</p><div className="jd-popup">{job.description||"No job description was supplied by the source."}</div><button className="secondary-button" onClick={()=>setModal(null)}>Close</button></>}{modal==="personal"&&active&&<><span className="eyebrow">PERSONALIZED EMAIL</span><h2>{active.name}</h2>{!drafts[active.id]?<p>Preparing a personalized email…</p>:<><label>Subject<input value={drafts[active.id].subject} onChange={e=>setDrafts(x=>({...x,[active.id]:{...x[active.id],subject:e.target.value}}))}/></label><label>Message<textarea className="message-box" value={drafts[active.id].body} onChange={e=>setDrafts(x=>({...x,[active.id]:{...x[active.id],body:e.target.value}}))}/></label><div className="modal-actions"><button className="secondary-button" onClick={()=>setModal(null)}>Close</button><button className="primary-button" disabled={busy||!active.email} onClick={()=>send([active.email!],drafts[active.id])}>Review & Send</button></div></>}</>}{modal==="common"&&<><span className="eyebrow">COMMON EMAIL · BCC</span><h2>Reach all selected contacts</h2>{!common?<p>Preparing one common email for {people.filter(p=>p.email).length} contacts…</p>:<><p>Recipients will be placed in BCC so they cannot see each other.</p><label>Subject<input value={common.subject} onChange={e=>setCommon({...common,subject:e.target.value})}/></label><label>Message<textarea className="message-box" value={common.body} onChange={e=>setCommon({...common,body:e.target.value})}/></label><div className="modal-actions"><button className="secondary-button" onClick={()=>setModal(null)}>Close</button><button className="primary-button" disabled={busy} onClick={()=>send(people.map(p=>p.email).filter(Boolean) as string[],common)}>Review & Send to All</button></div></>}</>}</div></div>}</main>}
